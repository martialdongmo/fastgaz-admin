export interface DeliveryResponse {
  id: number;          // Long → number
  driverName: string;  // String → string
  busy: boolean;       // boolean → boolean
  latitude: number;    // double → number
  longitude: number;   // double → number
}
