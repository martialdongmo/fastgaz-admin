export interface ProductResponse {
  id: number;           // Long → number
  name: string;         // String → string
  price: number;        // double → number
  quantity: number;     // int → number
  alertThreshold: number; // int → number
  storeId: number;      // Long → number
}
